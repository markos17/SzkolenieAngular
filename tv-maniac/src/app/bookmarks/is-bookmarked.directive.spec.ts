import { IsBookmarkedDirective } from './is-bookmarked.directive';

describe('IsBookmarkedDirective', () => {
  it('should create an instance', () => {
    const directive = new IsBookmarkedDirective();
    expect(directive).toBeTruthy();
  });
});
